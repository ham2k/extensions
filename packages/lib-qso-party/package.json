{
  "name": "@ham2k/lib-qso-party",
  "version": "0.1.0",
  "description": "The shared QSO-party engine: scoring, exchange handling and submittable files for state, provincial and regional QSO parties",
  "author": "Sebastian Delmont <sd@ham2k.com>",
  "license": "MIT",
  "type": "module",
  "//exports": [
    "Consumed as TypeScript source: the only consumers are extensions, which",
    "esbuild bundles, so there is no build step and no dist/ to point at."
  ],
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --test 'src/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "//dependencies": [
    "lib-qson-cabrillo is a local path until the Cabrillo WRITER it now carries",
    "is published; it becomes ^1.2.0 then. The host bundles its own copy of",
    "this library, so an extension needs the published version to be at least",
    "that before the writer is reachable at runtime."
  ],
  "dependencies": {
    "@ham2k/lib-qson-cabrillo": "file:../../../ham-js-libs/packages/lib-qson-cabrillo"
  },
  "//peerDependencies": [
    "The SDK is provided by the extension that consumes this package: its own",
    "bundle carries one copy, rewritten to the host's instance at build time."
  ],
  "peerDependencies": {
    "@ham2k/extension-sdk": "^0.2.0"
  },
  "devDependencies": {
    "@ham2k/extension-sdk": "^0.2.0"
  },
  "engines": {
    "node": ">=24"
  }
}
