{
  "name": "@ham2k/ext-pga",
  "version": "0.2.1",
  "private": true,
  "description": "Ham2K Logger extension: Polish Gmina Award",
  "author": "Sebastian Delmont <sd@ham2k.com>",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "pack": "npm run build && h2kext-pack build --force-name"
  },
  "//devDependencies": [
    "The libraries the host carries are DECLARED in manifest.json and rewritten",
    "to the host's instance at build time, so the bundle needs none of them",
    "installed. `tsc` does: it resolves every bare specifier the source names,",
    "and these are the ones it names."
  ],
  "devDependencies": {
    "@ham2k/extension-sdk": "^0.6.0",
    "@ham2k/extension-tools": "^0.6.0",
    "esbuild": "^0.25.5"
  },
  "engines": {
    "node": ">=24"
  }
}
