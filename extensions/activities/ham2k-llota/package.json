{
  "name": "@ham2k/ext-llota",
  "version": "0.2.1",
  "private": true,
  "description": "Ham2K Logger extension: Lakes and Lagoons on the Air",
  "author": "Sebastian Delmont <sd@ham2k.com>",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --import ../../../scripts/sdk-node-resolve.mjs --experimental-strip-types --test 'src/**/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "pack": "npm run build && h2kext-pack build --force-name"
  },
  "//devDependencies": [
    "The libraries the host carries are DECLARED in manifest.json and rewritten",
    "to the host's instance at build time, so the bundle needs none of them",
    "installed. The unit tests do: `node --test` runs the source as it is, with",
    "Node's own resolver, so every bare specifier it reaches has to be on disk.",
    "These are the ones this extension's own tests reach through the SDK."
  ],
  "devDependencies": {
    "@ham2k/extension-sdk": "^0.3.0",
    "@ham2k/extension-tools": "^0.3.0",
    "@ham2k/lib-country-files": "^1.0.202605301",
    "@ham2k/lib-dxcc-data": "^1.0.1",
    "@ham2k/lib-geo-tools": "^1.1.1",
    "@ham2k/lib-operation-data": "^1.1.3",
    "esbuild": "^0.25.5",
    "i18next": "^23.11.5",
    "liquidjs": "^10.28.0"
  },
  "engines": {
    "node": ">=24"
  }
}
