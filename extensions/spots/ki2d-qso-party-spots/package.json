{
  "name": "@ham2k/ext-ki2d-qso-party-spots",
  "version": "0.1.0",
  "private": true,
  "description": "Ham2K Logger extension: QSO Party Hub and APRS Tracker spots",
  "author": "Sebastian Delmont <sd@ham2k.com>",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --import ../../../scripts/sdk-node-resolve.mjs --experimental-strip-types --test 'src/**/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "pack": "npm run build && h2kext-pack build"
  },
  "//devDependencies": [
    "The libraries the host carries are DECLARED in manifest.json and rewritten",
    "to the host's instance at build time, so the bundle needs none of them",
    "installed. The two named here are what the unit tests import directly —",
    "the feed parsers read a band and a mode off a frequency."
  ],
  "dependencies": {
    "@ham2k/qso-parties": "^0.1.0"
  },
  "devDependencies": {
    "@ham2k/extension-sdk": "^0.8.1",
    "@ham2k/extension-tools": "^0.6.0",
    "@ham2k/lib-format-tools": "^1.0.0",
    "@ham2k/lib-operation-data": "^1.0.0",
    "esbuild": "^0.25.5"
  },
  "engines": {
    "node": ">=24"
  }
}
